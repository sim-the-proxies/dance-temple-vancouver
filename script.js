const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open');document.body.classList.toggle('menu-open')});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');document.body.classList.remove('menu-open');menuButton?.setAttribute('aria-expanded','false')}));document.querySelectorAll('[data-ticket]').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();alert('Tickets for our first PNE dance will be released soon.')}));document.querySelector('#year').textContent=new Date().getFullYear();

const contactForm=document.querySelector('#contact-form');const formTarget=document.querySelector('[name="contact-target"]');const formStatus=document.querySelector('#form-status');let formSending=false;contactForm?.addEventListener('submit',event=>{if(contactForm.elements.website.value){event.preventDefault();return}formSending=true;const button=contactForm.querySelector('button');button.disabled=true;formStatus.className='form-note';formStatus.textContent='Sending your message…'});formTarget?.addEventListener('load',()=>{if(!formSending)return;formSending=false;contactForm.reset();const button=contactForm.querySelector('button');button.disabled=false;formStatus.className='form-note form-success';formStatus.textContent='Message sent. We’ll get back to you soon.'});
